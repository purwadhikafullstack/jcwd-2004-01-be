const { dbCon } = require("../connection");
require("dotenv").config();
const fs = require("fs");
const morgan = require("morgan");

const inputProduct = async(data) => {
  let data 
}

module.exports = {};
